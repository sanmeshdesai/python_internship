comments: 
- make code readable
- non executable code
- single line # multiline '''

identifier: 
- unique name given to variable, class, function
- starts with _ or char
- camel case or pascal case.

variable: 
- container that stores value
- int, string, float, bool etc.
- dynamic type no need to data type

data type:
- primitive: built in, immutable, int float string bool none
- non primitive: user defined, derive from primitive, list, set, tuple, dict

type() - check data type.
type casting 
- converting one data type into another

string concatination: 
- using + : same data type print('name' + name)
- using , : print('name', name, 'age', age) 
- f string: print(f"your, {name}")

Input from user
- a = input()
- default string data type

keyword : reserve words
- import keyword
  print(keyword.kwlist)

memry location : 
- id(a)

constant :
- dont have turly constant.  
- capital letters when want to treate as constant
- PI = 3.14

Immutabel : cannot change. primitive data types
Mutable: can change. Non primitive

Operator:
- arithmatic
    - +, -, *, /, %, //, **
- comparison
    - ==, !=, <=, >=, <, >
- logical
    - and, or, not
- assignment
    - +=, -=, *=, /=, %=
- bitwise
    - &, |, ~, ^, <<, >>

Ternary: 
- value_if_true if condition else value_if_false

short circuting: stop evaluating logical operations if result known.

membership operator :
in
not in

identity operator: 
is
is not

control statememts
- conditional statement
- looping